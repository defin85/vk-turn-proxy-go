## 1. Desktop shell contract
- [ ] 1.1 Define the desktop GUI workflows for profiles, session control, challenge handoff, and diagnostics export
- [ ] 1.2 Define how the desktop shell discovers, launches, and verifies a compatible local host

## 2. Desktop platform integration
- [ ] 2.1 Define desktop-specific packaging and sidecar lifecycle behavior for Windows, macOS, and Linux
- [ ] 2.2 Define tray, notifications, and browser handoff behavior without changing runtime semantics

## 3. Verification
- [ ] 3.1 Add integration coverage for desktop shell to local host interactions
- [ ] 3.2 Run `openspec validate add-02-desktop-gui-shell --strict --no-interactive`
