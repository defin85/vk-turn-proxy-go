## 1. Observability contract

- [ ] 1.1 Define stable structured-log fields for provider resolution, session lifecycle, and transport failures.
- [ ] 1.2 Define the first runtime metric set and allowed label dimensions.

## 2. Instrumentation

- [ ] 2.1 Instrument client runtime startup, shutdown, and failure paths.
- [ ] 2.2 Instrument server runtime and forwarded traffic counters.
- [ ] 2.3 Expose the documented metrics surface for long-running binaries.

## 3. Verification and docs

- [ ] 3.1 Add tests or fixtures that assert key log/metric outputs and redaction behavior.
- [ ] 3.2 Document the observability surface and operator workflow.
