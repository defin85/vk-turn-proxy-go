## 1. Desktop shell contract
- [x] 1.1 Define the desktop GUI workflows for profiles, session control, challenge handoff, and diagnostics export
- [x] 1.2 Define how the desktop shell discovers, launches, and verifies a compatible local host

## 2. Desktop platform integration
- [x] 2.1 Define desktop-specific packaging and sidecar lifecycle behavior for Windows, macOS, and Linux
- [x] 2.2 Define tray, notifications, and browser handoff behavior without changing runtime semantics

## 3. Verification
- [x] 3.1 Add integration coverage for desktop shell to local host interactions
- [x] 3.2 Run `openspec validate add-02-desktop-gui-shell --strict --no-interactive`
