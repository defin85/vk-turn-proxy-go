## 1. Observability contract

- [x] 1.1 Define stable structured-log fields for provider resolution, session lifecycle, and transport failures.
- [x] 1.2 Define the first runtime metric set and allowed label dimensions.

## 2. Instrumentation

- [x] 2.1 Instrument client runtime startup, shutdown, and failure paths.
- [x] 2.2 Instrument server runtime and forwarded traffic counters.
- [x] 2.3 Expose the documented metrics surface for long-running binaries.

## 3. Verification and docs

- [x] 3.1 Add tests or fixtures that assert key log/metric outputs and redaction behavior.
- [x] 3.2 Document the observability surface and operator workflow.
