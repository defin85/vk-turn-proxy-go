## 1. Contract
- [x] 1.1 Define typed challenge completion modes and browser-return metadata in the local control-plane contract
- [x] 1.2 Define when mobile shells may auto-continue and when they must keep explicit manual confirmation

## 2. Mobile Browser Handoff
- [x] 2.1 Define Android and iOS return-signal handling for browser-mediated challenge flows
- [x] 2.2 Define the one-shot auto-resume policy and the fallback manual confirmation UX
- [x] 2.3 Define copy and UI semantics that distinguish browser launch from post-browser confirmation

## 3. Verification
- [x] 3.1 Add contract and shell coverage for app-return-compatible challenges and fallback manual confirmation
- [x] 3.2 Run `openspec validate update-14-mobile-browser-return-and-auto-resume --strict --no-interactive`
