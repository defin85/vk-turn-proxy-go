interface:
  display_name: "VK Turn Desktop Shell"
  short_description: "Product-specific desktop shell rules for vk-turn-proxy-go"
  default_prompt: "Use $vk-turn-desktop-shell to shape the vk-turn-proxy-go desktop shell around its fail-closed runtime and workflow contract."
dependencies:
  tools:
    - type: "mcp"
      value: "dart"
      description: "Official Dart and Flutter MCP server"
policy:
  allow_implicit_invocation: false
