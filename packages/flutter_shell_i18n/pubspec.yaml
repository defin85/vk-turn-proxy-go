name: flutter_shell_i18n
description: "Shared Flutter shell localization package for vk-turn-proxy-go."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  slang: ^4.14.0
  slang_flutter: ^4.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
